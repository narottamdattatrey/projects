(function() {
    //srict mode for iife
    'use strict';
    var webapp = angular.module('project');

    /** single page route configuration */
    webapp.config($routerProvider, $http, $stateProvider) {

    }
})();
